Creation du source !

En lancant notre programme il attend un argument 1 un path vers un fichier qui va etre copie dans
./backups + 'av[1]'. Le programme est lance avec les droits de level09 donc on veut lui faire
afficher son ~/.pass (home/users/level09/.pass).

Donc testons :
level08@OverRide:~$ ~/level08 /home/users/level09/.pass
ERROR: Failed to open ./backups//home/users/level09/.pass

Le programme essayer de creer un fichier et d'ecricre le fichier .pass dans
'./backups//home/users/level09/' sauf qu'il ne pas acceder apres './backups/' car il n'existe pas de
dossier home puis users puis level09. La solution serait de creer tous ces dossiers dans backups.
Mais on n'a pas les droit d'ecriture.

Utilisant un path relatif "./" on peut executer le programme depuis n'importe ou. Le seul endroit ou
a les droits d'ecriture c'est dans /tmp. Donc on va creer notre chemin grace a la commande `mkdir -p
backups/home/users/level09/`

donc dans tmp on peut lancer notre binaire

~/level08 /home/users/level09/.pass

Aucun message d'erreur 🤑

cat backups/home/users/level09/.pass
fjAwpJNs2vvkFLRebEvAQ2hFZ4uQBWfHRsP62d8S
